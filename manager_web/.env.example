# Base URL of the BotCircuits Manager backend (FastAPI, `botcircuits-manager`).
# The web calls this for auth + session traces.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8700

# GitHub link shown in the top bar.
NEXT_PUBLIC_GITHUB_URL=https://github.com/botcircuits-ai/botcircuits-agent
