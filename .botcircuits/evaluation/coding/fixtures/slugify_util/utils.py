"""Small string utilities."""


def shout(text: str) -> str:
    """Uppercase a string. (Existing, working helper — must keep working.)"""
    return text.upper()


# TODO(task): add a `slugify(text)` helper here.
